{
  "name": "customer-reviews-app",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:src && npm run build:mock",
    "build:src": "tsc -p tsconfig.json",
    "build:mock": "tsc mock-api/handler.ts --outDir dist/mock-api --module commonjs --target ES2020 --esModuleInterop --resolveJsonModule",
    "mock-api": "ts-node mock-api/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.668.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
